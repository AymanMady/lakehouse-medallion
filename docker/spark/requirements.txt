# Python libs available inside every Spark container (driver + workers)
importlib_metadata==8.5.0
pandas==2.2.3
pyarrow==17.0.0
python-dotenv==1.0.1
structlog==24.4.0
boto3==1.35.54
# Integration tests run INSIDE this image: testing against a different
# PySpark than the one the jobs use would test a different stack.
pytest==8.3.3
